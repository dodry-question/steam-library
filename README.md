<<<<<<< HEAD
# Steam Library Manager

Веб-приложение для управления библиотекой Steam с AI-рекомендациями игр.

## Возможности

- 📊 Просмотр библиотеки Steam с актуальными ценами и скидками
- 🤖 AI-рекомендации игр на основе вашего профиля
- 🔍 Поиск и сортировка игр
- 📱 Адаптивный дизайн (desktop + mobile)
- 🎮 Просмотр библиотек друзей
- ⏱️ Статистика времени в играх

## Технологии

**Backend:**
- FastAPI (асинхронный веб-фреймворк)
- SQLModel (ORM для работы с БД)
- httpx (асинхронные HTTP запросы)
- SQLite (база данных)

**Frontend:**
- Vanilla JavaScript (модульная архитектура)
- CSS3 (адаптивный дизайн)
- Jinja2 (шаблонизатор)

**AI:**
- VseGPT API (GPT-4o-mini)
- Groq API

## Установка

### 1. Клонирование репозитория

```bash
git clone <your-repo-url>
cd SteamProject
```

### 2. Установка зависимостей

=======
# Steam Smart Library

Веб-приложение для анализа библиотеки Steam. Проект позволяет просматривать свою коллекцию игр, актуальные цены в магазине, время в игре, а также получать персонализированные рекомендации с помощью большой языковой модели (LLM).


---

## Путеводитель по проекту (Интерфейс)

Ниже представлен обзор основного функционала сайта.

<details>
  <summary><b> Нажмите, чтобы посмотреть общий интерфейс сайта</b></summary>
  <br>
  <img width="1917" height="937" alt="image" src="https://github.com/user-attachments/assets/53ca8b0c-d38a-4025-9a5f-d7f0a99c9602" />
</details>

### 1. Загрузка и анализ библиотеки
После входа (через безопасный Steam OpenID или просто по ссылке на открытый профиль) приложение вытягивает весь список ваших игр. 

<details>
  <summary><b> Нажмите, чтобы посмотреть загруженную библиотеку</b></summary>
  <br>
  <img width="1915" height="939" alt="image" src="https://github.com/user-attachments/assets/a05361e8-beae-4c8f-af6d-22c4a5cff751" />
</details>


### 2. Умные рекомендации (AI Советник)
Встроенная нейросеть анализирует ваш профиль и предлагает игры. 
Есть два режима работы:
*   **Глобальный:** ИИ смотрит на топ-15 ваших любимых игр и подбирает новые, опираясь на выбранное настроение (например, "Скрытые жемчужины" или "Медитативное").
*   **Локальный (Поиск похожего):** Вы можете выделить конкретные игры в библиотеке (Shift + Клик на ПК или долгое нажатие на телефоне), и ИИ найдет игры, похожие именно на этот набор.

<details>
  <summary><b> Нажмите, чтобы посмотреть работу AI Советника</b></summary>
  <br>
  <img width="1858" height="381" alt="image" src="https://github.com/user-attachments/assets/d148d74d-5202-4515-b3f4-9d724312b99c" />
</details>

### 3. Мобильная адаптация
Интерфейс полностью адаптирован под смартфоны. Реализована кастомная физика скролла для витрины фич, компактный вид "3 в ряд" для карточек игр и поддержка сенсорного выделения (долгий тап).

<details>
  <summary><b> Нажмите, чтобы посмотреть мобильный интерфейс</b></summary>
  <br>
  <p align="center">
    <img width="350" alt="Мобильный интерфейс" src="https://github.com/user-attachments/assets/c91e6d73-2019-41b9-947b-5bfb4b4b5ae5" />
  </p>
</details>

---

## Как это работает под капотом

Проект построен на связке **Python (FastAPI)** и чистого **JavaScript**.

### Фоновая загрузка и кэширование (База данных)
Чтобы не превышать лимиты (Rate Limits) официального API Steam и ускорить загрузку, реализована система локального кэширования:
1. При запросе библиотеки бэкенд быстро отдает список ID игр пользователя.
2. Фронтенд рисует "скелеты" карточек.
3. Бэкенд через асинхронный генератор (StreamingResponse) проверяет SQLite базу (`games.db`). Если данные свежие (сохранены после последнего обновления магазина Steam в 10:10 PT), они отдаются из БД.
4. Если игры в базе нет, сервер делает запрос к Steam Store API, парсит цену, скидки и жанры, сохраняет в базу и отправляет на фронтенд. 

### Интеграция с LLM (Генерация рекомендаций)
За рекомендации отвечает модель `gpt-4o-mini` (через API-провайдера). 
Как строится промпт:
*   Система собирает названия любимых игр пользователя.
*   **Защита от дублей:** В промпт передается список из топ-300 игр пользователя с жестким запретом (system prompt) советовать то, что уже куплено.
*   **Защита от повторений:** Сохраняется история выданных рекомендаций в рамках сессии, чтобы при повторном нажатии ИИ предлагал новые варианты.
*   **Парсинг:** Модель проинструктирована возвращать ответ строго в формате JSON. Бэкенд валидирует этот JSON и ищет предложенные названия в базе Steam, чтобы вернуть на фронтенд красивые кликабельные карточки с реальными обложками.

### Система авторизации
Доступно два метода:
1.  **OpenID (Официальный вход):** Пользователь авторизуется на стороне Steam, сервер получает только публичный `SteamID64`.
2.  **По ссылке (Vanity URL):** Пользователь может ввести кастомную ссылку на профиль (например, `steamcommunity.com/id/nickname`). Бэкенд использует метод `ResolveVanityURL` для получения `SteamID64`.

---


## Технологический стек

*   **Backend:** Python 3.10+, FastAPI
*   **Database:** SQLite, SQLModel (ORM)
*   **Frontend:** HTML5, CSS3, Vanilla JavaScript
*   **External APIs:** Steam Web API, Steam Storefront API, VseGPT API (LLM)
*   **Deployment:** Amvera Cloud

---

## Локальный запуск (Для разработчиков)

### 1. Подготовка окружения

Клонируйте репозиторий и перейдите в папку проекта:
```bash
git clone https://github.com/dodry-question/steam-library.git
cd steam-library
```

Создайте и активируйте виртуальное окружение:

* **Для Windows (CMD/PowerShell):**
  ```bash
  python -m venv venv
  venv\Scripts\activate
  ```
* **Для macOS/Linux (GitBash):**
  ```bash
  python -m venv venv
  source venv/bin/activate
  ```

Установите необходимые зависимости:
>>>>>>> 23db035eb4262d67151ecdc71c0501ad685250ee
```bash
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

Перед запуском серверу нужны API ключи. Создайте файл `.env` в корневой папке проекта (рядом с `main.py`) и скопируйте туда следующие параметры, подставив свои значения:

```env
# --- Основные настройки ---
STEAM_API_KEY=ваш_ключ_от_steam
MY_DOMAIN=http://localhost:8000

# --- Основная LLM (VseGPT) ---
VSEGPT_API_KEY=ваш_ключ_от_vsegpt
VSEGPT_BASE_URL=https://api.vsegpt.ru/v1

# --- Альтернативные провайдеры LLM (Опционально) ---
GROQ_API_KEY=ваш_ключ_от_groq
OPENROUTER_API_KEY=ваш_ключ_от_openrouter
```
*(Ключ Steam можно получить на [портале разработчиков Steam](https://steamcommunity.com/dev/apikey))*

### 3. Запуск сервера

Когда зависимости установлены и ключи добавлены, запустите приложение:
```bash
python main.py
```

<<<<<<< HEAD
Или через uvicorn:

```bash
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

Приложение будет доступно по адресу: http://localhost:8000

## Структура проекта

```
SteamProject/
├── main.py                 # Основной файл приложения
├── games.db               # База данных SQLite
├── app.log                # Логи приложения
├── requirements.txt       # Зависимости Python
├── .env                   # Переменные окружения (не в Git!)
├── .gitignore            # Игнорируемые файлы
├── static/
│   ├── css/
│   │   ├── main.css      # Основные стили
│   │   └── mobile.css    # Мобильные стили
│   ├── js/
│   │   ├── app.js        # Главный файл приложения
│   │   ├── api.js        # API запросы
│   │   ├── ui.js         # Работа с интерфейсом
│   │   └── selection.js  # Логика выделения игр
│   └── favicon.png       # Иконка сайта
└── templates/
    └── index.html        # HTML шаблон
```

## Безопасность

### CORS
Настроен для конкретных доменов. Для локальной разработки разрешены:
- http://localhost:8000
- http://localhost:8001
- http://127.0.0.1:8000
- http://127.0.0.1:8001

Для продакшена добавьте свой домен в `main.py`:

```python
allow_origins=[
    "https://your-domain.com",
    "http://localhost:8000"
]
```

### Rate Limiting
AI запросы ограничены: **10 запросов в минуту на IP**.

Настройка в `main.py`:
```python
AI_RATE_LIMIT = 10  # запросов
AI_RATE_WINDOW = 60  # секунд
```

### Переменные окружения
**ВАЖНО:** Никогда не коммитьте `.env` файл в Git!

Проверьте `.gitignore`:
```
.env
*.db
app.log
```

## Деплой на Amvera

1. Создайте проект на https://amvera.ru
2. Подключите Git репозиторий
3. Добавьте переменные окружения в панели Amvera:
   - `STEAM_API_KEY`
   - `GROQ_API_KEY`
   - `VSEGPT_API_KEY`
   - `VSEGPT_BASE_URL`
   - `MY_DOMAIN` (ваш домен на Amvera)

4. Обновите CORS в `main.py`:
```python
allow_origins=[
    "https://your-app.amvera.io",
    "http://localhost:8000"
]
```

## Логирование

Логи сохраняются в файл `app.log` и выводятся в консоль.

Уровни логирования:
- `INFO` - общая информация
- `WARNING` - предупреждения
- `ERROR` - ошибки

Просмотр логов:
```bash
tail -f app.log
```

## Разработка

### Добавление новых функций

1. **Backend (Python):**
   - Добавьте эндпоинт в `main.py`
   - Используйте async/await для асинхронных операций
   - Добавьте логирование

2. **Frontend (JavaScript):**
   - API запросы → `static/js/api.js`
   - UI логика → `static/js/ui.js`
   - Стили → `static/css/main.css`

### Тестирование

Запустите приложение локально:
```bash
python main.py
```

Откройте в браузере: http://localhost:8000

## Известные проблемы

- Steam API может блокировать при частых запросах (используется rate limiting)
- Региональные ограничения игр (показывается цена из США)
- AI может не найти игру, если название неточное

## Лицензия

Этот проект создан в образовательных целях.

## Автор

Создано с помощью Gemini и Claude AI.

## Поддержка

Если возникли проблемы:
1. Проверьте логи в `app.log`
2. Убедитесь, что все ключи API корректны
3. Проверьте, что профиль Steam открыт (настройки приватности)
=======
Сайт будет доступен в браузере по адресу: **http://localhost:8000**
>>>>>>> 23db035eb4262d67151ecdc71c0501ad685250ee
